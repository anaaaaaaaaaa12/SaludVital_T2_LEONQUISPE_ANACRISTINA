server:
  port: 8084 
spring:
  application:
    name: auth-service
  datasource:
    url: jdbc:mysql://localhost:3306/cibertec_auth 
    username: root
    password: mysql
  jpa:
    hibernate:
      ddl-auto: update 
    show-sql: true
    properties:
      hibernate:
        format_sql: true

security:
  jwt:
    secret: MiSuperClaveSegura1234567890!@#$
    expiration: 3600000 
